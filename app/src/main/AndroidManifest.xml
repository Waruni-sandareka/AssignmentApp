<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
            android:usesCleartextTraffic="true"
            android:networkSecurityConfig="@xml/network_security_config"
            android:allowBackup="true"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.FOTNewsMobileApp"
            tools:targetApi="31">
        <activity
                android:name=".MainActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- LoginActivity declaration -->
        <activity
                android:name=".view.LoginActivity"
                android:exported="true" />

        <!-- SignUpActivity declaration -->
        <activity
                android:name=".view.SignupActivity"
                android:exported="true" />

        <!-- NewsActivity declaration -->
        <activity
                android:name=".view.NewsActivity"
                android:exported="true" />

        <activity
                android:name=".view.UserActivity"
                android:exported="true" />

        <activity
                android:name=".view.DeveloperActivity"
                android:exported="true" />

        <!-- Add other activities like SignupActivity, NewsActivity here if needed -->

    </application>

</manifest>